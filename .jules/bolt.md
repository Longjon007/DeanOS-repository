# Bolt's Journal

## 2024-05-24 - Initial Entry
**Learning:** Performance optimization requires measurement. Without tests or a running environment, rely on proven patterns (like minimizing render allocations).
**Action:** Always check for `renderItem` and `keyExtractor` hoisting in React Native lists.
