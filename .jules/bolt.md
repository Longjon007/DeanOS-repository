## 2024-05-24 - Mobile Environment Constraints
**Learning:** The `app/` directory lacks a configured test runner (Jest/Expo), making automated performance verification impossible for mobile changes.
**Action:** Future optimizations in `app/` must rely on manual verification or require setting up the test harness first.
